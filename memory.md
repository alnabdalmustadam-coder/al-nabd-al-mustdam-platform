# ذاكرة المشروع والمحادثة (memory.md)

هذا الملف مخصص لتسجيل سياق المحادثة الحالية، هيكلية النظام، التغييرات التي تم تنفيذها، وقائمة المهام المستقبلية ليكون مرجعاً دائماً للمطور والمساعد الذكي لضمان استمرارية التطوير بكفاءة عالية وبدون فقدان السياق.

---

## 🎯 أهداف المشروع العامة
* بناء وتطوير منصة **النبض المستدام (Sustainable Pulse / SustainPulse)** التعليمية والتدريبية التابعة لـ **TTi**.
* تقديم تجربة مستخدم (UX) استثنائية وبريميوم للمتدربين والشركات مع دعم كامل للغة العربية والخطوط الاحترافية (Cairo).
* التكامل السلس مع نظام إدارة العلاقات مع العملاء **GoHighLevel (GHL)** ونظام **Supabase** لإدارة المستخدمين وقواعد البيانات.
* الامتثال لمعايير المركز الوطني للتعليم الإلكتروني (**NELC**) وتسجيل بيانات وبيئات التعلم (LRS/xAPI).

---

## 🛠️ البنية التقنية والنظام (Technical Stack & Architecture)
* **الإطار البرمجي:** React 19 / Next.js (App Router).
* **إدارة الجلسات والهوية (Authentication):** Supabase Auth + استرداد بيانات المتدرب ديناميكياً من profiles المتكاملة مع GoHighLevel.
* **التصميم والتنسيق:** Tailwind CSS مع خطوط Google Fonts الاحترافية (خط Cairo كخط أساسي افتراضي للمنصة: `var(--font-cairo)`).
* **المكتبات الحركية:** Framer Motion للميكرو-أنيميشنز والتأثيرات التفاعلية الفاخرة.
* **إدارة الحالات وبيانات xAPI:** إرسال واسترجاع statements لتتبع تقدم الطلاب والتقييمات المتكاملة مع LRS.

---

## 🚀 التغييرات والتحسينات الأخيرة التي تم تنفيذها

### 1. تحسينات الهيدر وشريط التنقل (Navbar & Typography)
* **إعادة الأيقونات الفاخرة:** تم استعادة وتنسيق أيقونات Lucide بجانب روابط القائمة الرئيسية على شاشات الكمبيوتر بشكل بريميوم وجميل.
* **تطوير وتحسين الخطوط:** تم تطبيق خط **Cairo** بشكل صريح على كافة عناصر الهيدر، وترقية أوزان النصوص إلى `font-black` (العريض جداً) للروابط والأزرار الرئيسية لزيادة الوضوح والجاذبية البصرية، واستخدام `font-medium` للروابط الفرعية.
* **دمج أزرار تسجيل الدخول:** تم التخلص من تكرار الأزرار ("تسجيل دخول" و "سجّل الآن") ودمجها في زر موحد وبارز يتكيف ديناميكياً مع الخلفية (الفاتحة في الصفحات العادية، والمظلمة المتوهجة في صفحات تسجيل الدخول).
* **إزالة الوصلات الزائدة:** حذف رابط "احجز استشارة" لتقليل التشتت البصري والحفاظ على انسيابية الهيدر.

### 2. إعادة هيكلة الكارت الجانبي للوحة التحكم (Dashboard Sidebar)
* **الارتفاع والثبات (Desktop Sticky 80vh):**
  * تم تعديل الكارت الجانبي على أجهزة الكمبيوتر ليأخذ ارتفاعاً ثابتاً يعادل `80vh` (`lg:h-[80vh]`).
  * جعل الكارت ثابتاً مع السكرول (`sticky top-28`) لضمان سهولة التنقل الفوري بين أقسام لوحة التحكم.
* **توزيع المحتوى الذكي (Flex Layout):**
  * عزل محتوى التبويبات بالأعلى وتفعيل التمرير الداخلي الهادئ (`overflow-y-auto no-scrollbar`) في حال زاد عدد التبويبات.
  * تثبيت الشعار وزر تسجيل الخروج في أسفل الكارت بشكل بريميوم ومستقل.
* **إضافة الشعار الفاخر (Premium Logo Container):**
  * تصميم حاوية زجاجية فاخرة للشعار فوق زر تسجيل الخروج (`bg-slate-50/60 rounded-2xl border border-slate-100`) مع تأثير حركي ناعم وتكبير طفيف عند التمرير (Hover).
* **تثبيت زر تسجيل الخروج بالأسفل:** وضع زر تسجيل الخروج الأحمر في نهاية القائمة الجانبية بشكل أنيق لسهولة الوصول.
* **التوافق التام مع الجوال:** حصر التعديلات الهيكلية المعقدة على شاشات الكمبيوتر فقط (`lg:`) والحفاظ على المظهر المتناسق والتلقائي للجوال.

### 3. إصلاح ثغرات تسجيل الخروج وتوجيه الروابط (Auth & Redirect Fixes)
* **إنهاء الجلسة الفعلي (Supabase Session Sign Out):**
  * تم اكتشاف أن تسجيل الخروج كان يمسح الكوكيز التقليدية فقط دون إنهاء جلسة Supabase الفعلية، مما يُبقي المستخدم مسجلاً.
  * **الحل:** تم تحديث نقطة النهاية للـ API (`app/api/auth/logout/route.ts`) لاستدعاء `supabase.auth.signOut()` ومسح التوكنز بالكامل.
* **إصلاح خطأ صفحة 404:**
  * كان النظام يقوم بتوجيه المستخدمين بعد تسجيل الخروج أو عند انتهاء الجلسة إلى المسار `/login` غير الموجود في الهيكلية الجديدة لـ Next.js.
  * **الحل:** تم تصحيح مسارات التوجيه في لوحة التحكم وعملية تسجيل الخروج لتوجه المستخدمين بالكامل إلى المسار الصحيح والموجود وهو `/auth/login`.

---

## 📌 ملاحظات تقنية هامة للمطورين (Critical Technical Notes)
* **تجنب مشاكل تشفير الأحرف في الهيدر (ByteString Crash):**
  * تم تصميم Middleware ذكي في `middleware.ts` يقوم بتنظيف وترميز أي أحرف غير تابعة لـ ASCII (مثل الكوكيز أو العناوين المحتوية على أحرف عربية) تلقائياً لمنع خوادم الويب من الانهيار.
* **روابط GHL:**
  * روابط تسجيل الدخول والتسجيل الأساسية موجهة مباشرة لـ GoHighLevel المخصص لـ TTi عبر النطاق `https://members.nabdtraining.com/login` لضمان مزامنة بيانات الطلاب التلقائية.

---

## 📝 قائمة المهام القادمة والمستقبلية (Upcoming Roadmap)
- [ ] مراقبة أداء تسجيل الدخول الموحد وسرعة الاستجابة.
- [ ] تعزيز الميكرو-أنيميشنز داخل صفحات الكورسات الفردية.
- [ ] متابعة تقارير المركز الوطني للتعليم الإلكتروني وتأكيد عمل الـ xAPI Statements بكفاءة 100% لجميع المتدربين.
