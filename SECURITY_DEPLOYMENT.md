# خطة تفعيل تحسينات الأمان

هذه التعديلات تفشل بشكل آمن عند نقص الإعدادات. نفّذ الخطوات التالية قبل نشر النسخة.

## 1. تدوير المفاتيح المكشوفة سابقًا

احذف/دوّر القيم القديمة من لوحات الخدمات، ثم حدّث بيئة الاستضافة. حذفها من الملفات الحالية لا يكفي لأنها وُجدت في سجل Git:

- Supabase service-role key.
- Bunny Stream API key وToken Authentication key.
- GoHighLevel Private Integration token.

لا تضع أي مفتاح خادمي في متغير يبدأ بـ`NEXT_PUBLIC_`.

## 2. مراجعة الأدوار الحالية

التفويض يعتمد فقط على `auth.users.raw_app_meta_data.role`. ترقية مستخدم اعتمادًا على `profiles.role` غير آمنة لأن السياسات القديمة كانت تسمح بتعديل الملف الشخصي.

قبل تطبيق الترحيل:

1. راجع يدويًا قائمة المديرين والمدربين المعتمدين.
2. عيّن دورهم من لوحة Supabase أو تدفق إداري يستخدم service role.
3. تأكد أن القيم المسموحة هي `ADMIN` أو `INSTRUCTOR` أو `STUDENT`.

أي حساب لا يملك دورًا موثوقًا داخل `app_metadata` سيُعامل كطالب، وهذا مقصود كخيار آمن.

## 3. تطبيق SQL

خذ نسخة احتياطية لقاعدة البيانات، ثم طبّق بالترتيب:

1. `supabase-v5-missing-tables.sql` إذا كانت جداول الاختبارات والواجبات والمشروعات مستخدمة.
2. `supabase-marketplace-tables.sql` إذا كانت جداول سوق الخدمات مستخدمة.
3. `supabase/migrations/security-hardening-v1.sql` أخيرًا لإزالة أي سياسات قديمة متعارضة وتأمين الأدوار وwebhook replay protection.

لا تطبق ملفات `schema-v2.sql` و`schema-v3-student.sql` كترحيلات جديدة فوق قاعدة الإنتاج؛ هي مخططات تأسيسية قديمة ومتداخلة.

## 4. متغيرات البيئة

استخدم `.env.example` كقائمة مرجعية، واضبط خصوصًا:

- `GHL_WEBHOOK_SECRET`: سر عشوائي قوي يرسل كـBearer token أو `x-webhook-secret` لتدفقات GHL غير الموقعة بتوقيع Marketplace.
- `XAPI_BASIC_USERNAME` و`XAPI_BASIC_PASSWORD`.
- `ALLOWED_ORIGINS` و`NEXT_PUBLIC_SITE_URL` بالنطاقات الفعلية فقط.
- مفاتيح Supabase وBunny وبيانات البريد كمتغيرات خادمية في منصة الاستضافة.

## 5. حدود مقصودة حتى اكتمال التكاملات

- التسجيل الذاتي مسموح للدورات المجانية فقط. الدورة المدفوعة تحتاج webhook دفع موثوقًا أو تسجيلًا إداريًا.
- تقدم الدروس القادم من المتصفح لا يتجاوز 99% ولا يصدر شهادة.
- اكتمال الدورة والشهادة الأكاديمية يظلان إداريين/خادميين حتى إضافة تقييم خادمي أو تكامل دفع موثوق.
- مفاتيح Bunny لا تصل للمتصفح؛ الرفع يستخدم TUS بتوقيع قصير العمر.

## 6. تحقق ما بعد النشر

- اختبر أن مسارات الإدارة تعيد 401 بدون جلسة و403 للطالب.
- اختبر أن webhook غير الموقع وxAPI بدون Basic Auth يعيدان 401.
- اختبر مستخدمًا واحدًا لكل دور بعد تحديث JWT/تسجيل الدخول من جديد.
- اختبر رفع صورة وملف وفيديو بالأنواع والأحجام المسموحة والمرفوضة.
- راقب سجلات 401/403 وwebhook duplicates أول 24 ساعة.
